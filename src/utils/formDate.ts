/* En la carpeta utils incluir funciones unitarias como helpers
* o constantes que se pueden reutilizar en varios componentes o archivos.
En este caso, se incluye una función que formatea una fecha a un formato específico.
*/

/*
export const formatDate = (date: string): string => {
  const options = { year: 'numeric', month: 'long', day: 'numeric' };
  return new Date(date).toLocaleDateString(undefined, options);
};

*/
